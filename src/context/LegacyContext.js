/**
 * Legacy Context - Backward compatibility wrapper
 * This maintains the existing API while using the new modular contexts internally
 */
import { createContext } from "react";

export const Context = createContext();
